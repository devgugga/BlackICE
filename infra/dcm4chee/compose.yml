services:
  ldap:
    image: dcm4che/slapd-dcm4chee:2.6.10-34.3
    logging:
      driver: json-file
      options:
        max-size: "10m"
    environment:
      STORAGE_DIR: /storage/fs1
    volumes:
      - dcm4chee-ldap-data:/var/lib/openldap/openldap-data
      - dcm4chee-slapd-conf:/etc/openldap/slapd.d
    networks: [blackice]

  # Backend de dados exigido pelo Keycloak nesta baseline (não citado nas
  # Interfaces da Task 2, mas obrigatório: o compose oficial "secure" do
  # dcm4chee-arc-light usa MariaDB como banco do Keycloak desde a série 5.3x,
  # incluindo a versão do Keycloak pinada aqui — 25.0.6). Ver README.
  mariadb:
    image: mariadb:10.11.4
    logging:
      driver: json-file
      options:
        max-size: "10m"
    environment:
      MYSQL_ROOT_PASSWORD: ${KEYCLOAK_DB_ROOT_PASSWORD}
      MYSQL_DATABASE: keycloak
      MYSQL_USER: keycloak
      MYSQL_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
    volumes:
      - dcm4chee-mysql-data:/var/lib/mysql
    networks: [blackice]

  keycloak:
    image: dcm4che/keycloak:25.0.6
    logging:
      driver: json-file
      options:
        max-size: "10m"
    ports:
      - "8843:8843"
    environment:
      KC_HTTPS_PORT: 8843
      KC_HOSTNAME_BACKCHANNEL_DYNAMIC: 'true'
      # Keycloak 25.x usa KEYCLOAK_ADMIN/KEYCLOAK_ADMIN_PASSWORD para o bootstrap
      # do admin do realm master (a partir do Keycloak 26 passou a ser
      # KC_BOOTSTRAP_ADMIN_USERNAME/PASSWORD — não usar essas aqui).
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KC_DB: mariadb
      KC_DB_URL_DATABASE: keycloak
      KC_DB_URL_HOST: mariadb
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      KC_LOG: file
      # Nome do realm. Default oficial da imagem é "dcm4che"; era "dcm4chee".
      #
      # NUMA BASE VAZIA (cold start, volume novo) este valor basta: o
      # --import-realm cria o realm já com o nome daqui.
      #
      # NUMA BASE JÁ POPULADA — o caso deste ambiente — trocá-lo sozinho NÃO
      # funciona (spec 2026-08-07, Fase 2). `--import-realm` importa
      # dcm4che-realm.json, que embute 112 UUIDs literais NÃO templados por
      # ${REALM_NAME} — entre eles `defaultRole.id`. Como KEYCLOAK_ROLE.ID é
      # PK global, importar o mesmo arquivo uma segunda vez sob outro nome
      # colide e trava o boot do Keycloak. Verificado empiricamente.
      #
      # Por isso o realm foi renomeado IN PLACE pela Admin REST
      # (`PUT /admin/realms/<atual> {"realm":"blackice"}`), o que preserva
      # usuários, clients, mappers, role mappings e o login_theme. Este valor
      # só acompanha o nome real para que o import de boot encontre o realm já
      # existente e não tente criar outro.
      #
      # REVERTER exige os CINCO passos (os dois primeiros sozinhos deixam o
      # login quebrado) — receita completa na seção "Reversão" do spec
      # docs/superpowers/specs/2026-08-07-keycloak-same-origin-design.md:
      #   1. PUT /admin/realms/blackice {"realm":"dcm4chee"}
      #   2. renomear a role padrão default-roles-blackice ->
      #      default-roles-dcm4chee (GET a representação inteira, troque só o
      #      `name`, PUT de volta; ela viaja no claim realm_access.roles,
      #      lido por quarkus.oidc.roles.role-claim-path)
      #   3. este valor, nos DOIS serviços (`keycloak` e `arc`)
      #   4. REALM em infra/keycloak/configure-blackice.sh e o realm na URL de
      #      QUARKUS_OIDC_AUTH_SERVER_URL em infra/compose.apps.yml
      #   5. quarkus.oidc.auth-server-url na application.properties do backend
      #      — e como o Dockerfile.jvm copia o target/ pré-buildado, isso pede
      #      `./mvnw package` E `build --no-cache backend`, não só restart
      # Nenhum dado é perdido no caminho.
      REALM_NAME: blackice
      ARCHIVE_HOST: ${DCM4CHEE_HOST}
      KEYCLOAK_WAIT_FOR: "ldap:389 mariadb:3306"
      # DEV-ONLY: a imagem roda `kc.sh start` (modo produção), que cacheia temas
      # e templates. Sem estas três, editar theme.properties ou CSS não surte
      # efeito até um restart do container (~21s). Num deploy real, remover as
      # três e assar o tema dentro da imagem em vez de bind-montar.
      KC_SPI_THEME_CACHE_THEMES: 'false'
      KC_SPI_THEME_CACHE_TEMPLATES: 'false'
      KC_SPI_THEME_STATIC_MAX_AGE: '-1'
      # Same-origin (spec 2026-08-07): o Keycloak passa a ser servido pelo
      # Traefik em http://${APP_HOST}/auth. KC_HTTP_ENABLED liga o listener
      # HTTP em :8080 na rede interna; o listener HTTPS 8843 continua vivo,
      # porque é o backchannel do Archive.
      # ATENÇÃO: KC_HTTP_RELATIVE_PATH é o root path do SERVIDOR INTEIRO —
      # vale para a 8843 também. Todo consumidor precisa do /auth no endereço.
      KC_HTTP_ENABLED: 'true'
      KC_HTTP_RELATIVE_PATH: /auth
      # URL completa é exigida por KC_HOSTNAME_BACKCHANNEL_DYNAMIC (hostname v2).
      KC_HOSTNAME: http://${APP_HOST}/auth
      # NÃO é o que faz o proxy funcionar: com hostname fixo, as frontend URLs
      # saem dele, não dos headers. Isto é para o Keycloak confiar nos
      # X-Forwarded-* do Traefik ao resolver IP do cliente e esquema.
      KC_PROXY_HEADERS: xforwarded
    depends_on:
      - ldap
      - mariadb
    volumes:
      - dcm4chee-keycloak-data:/opt/keycloak/data
      # Tema de login do BlackICE. Read-only: o Keycloak não escreve aqui.
      # O caminho é relativo ao project directory do Compose — por padrão o
      # diretório do PRIMEIRO arquivo `-f` (ou de --project-directory, se
      # passado), não o cwd de onde o comando roda e não o diretório deste
      # arquivo. Aqui isso é infra/, porque toda invocação no repo lista
      # infra/compose.yml primeiro.
      - "./keycloak/themes/blackice:/opt/keycloak/themes/blackice:ro"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.auth.rule=Host(`${APP_HOST}`) && PathPrefix(`/auth`)"
      - "traefik.http.routers.auth.entrypoints=web"
      # priority > 1 é obrigatório: o router `spa` é Host(${APP_HOST}) com
      # priority=1 e casa com TUDO neste host, inclusive /auth.
      - "traefik.http.routers.auth.priority=10"
      - "traefik.http.services.auth.loadbalancer.server.port=8080"
    networks: [blackice]

  # Postgres do DCM4CHEE (Archive). Nomeado "arc-db" (conforme Interfaces da
  # Task 2) em vez do "db" oficial; para o Archive encontrá-lo é preciso
  # apontar POSTGRES_HOST=arc-db explicitamente no serviço `arc` abaixo —
  # o default embutido na imagem do Archive é o hostname "db".
  arc-db:
    image: dcm4che/postgres-dcm4chee:17.4-34
    logging:
      driver: json-file
      options:
        max-size: "10m"
    environment:
      POSTGRES_DB: ${DCM4CHEE_DB}
      POSTGRES_USER: ${DCM4CHEE_DB_USER}
      POSTGRES_PASSWORD: ${DCM4CHEE_DB_PASSWORD}
    volumes:
      - dcm4chee-db-data:/var/lib/postgresql/data
    networks: [blackice]

  arc:
    image: dcm4che/dcm4chee-arc-psql:5.34.3-secure
    environment:
      POSTGRES_HOST: arc-db
      POSTGRES_DB: ${DCM4CHEE_DB}
      POSTGRES_USER: ${DCM4CHEE_DB_USER}
      POSTGRES_PASSWORD: ${DCM4CHEE_DB_PASSWORD}
      AUTH_SERVER_URL: https://keycloak:8843/auth
      UI_AUTH_SERVER_URL: https://${DCM4CHEE_HOST}:8843/auth
      # Tem de casar com o REALM_NAME do serviço `keycloak` acima — provider-url
      # é ${env.AUTH_SERVER_URL}/realms/${env.REALM_NAME}, resolvido a cada
      # boot; um mismatch quebra a validação de token do Archive em silêncio.
      REALM_NAME: blackice
      WILDFLY_CHOWN: /storage
      WILDFLY_WAIT_FOR: "ldap:389 arc-db:5432 keycloak:8843"
    depends_on:
      - ldap
      - keycloak
      - arc-db
    volumes:
      - dcm4chee-wildfly-data:/opt/wildfly/standalone
      - dcm4chee-storage:/storage
    networks: [blackice]

volumes:
  dcm4chee-ldap-data:
  dcm4chee-slapd-conf:
  dcm4chee-mysql-data:
  dcm4chee-keycloak-data:
  dcm4chee-db-data:
  dcm4chee-wildfly-data:
  dcm4chee-storage:
